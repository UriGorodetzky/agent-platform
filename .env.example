# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# These are placeholders — never put real secrets in THIS file.

# Needed later, when we add API-based agents (Phase 2+):
# OPENAI_API_KEY=sk-your-key-here
# GOOGLE_API_KEY=your-key-here

# The Claude Code CLI uses its own login, so no key is needed for ClaudeAgent.
